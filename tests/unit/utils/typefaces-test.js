import typefaces from 'typeswipe/utils/typefaces';
import { module, test } from 'qunit';

module('Unit | Utility | typefaces');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = typefaces();
  assert.ok(result);
});
