import fisherYates from 'typeswipe/utils/fisher-yates';
import { module, test } from 'qunit';

module('Unit | Utility | fisher yates');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fisherYates();
  assert.ok(result);
});
